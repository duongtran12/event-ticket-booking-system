spring:
  application:
    name: eventticket

  datasource:
    url: jdbc:mysql://localhost:3307/event_ticket_db
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080